# Loopdeck Short Feed Test Prototype

This repository is a test prototype for a mobile short-form video feed UI.

It is not affiliated with Instagram or Meta. The implementation uses an original visual design while demonstrating common short-form feed interactions such as vertical swiping, likes, comments, sharing, saving, following, and mute state.

Open `index.html` directly in a browser to run the prototype.
